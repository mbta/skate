import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import "../../css/color/_root.scss"
import {serviceColors, mbtaColors, themeColors, allColors} from "./colors";

<Meta title="Colors" />

### Semantic Colors
We use a subset of [all colors](#all-colors) to create a smaller color palette
for generating color schemes.

<ColorPalette>
  <ColorItem
    title="Theme Colors"
    colors={themeColors}
  />
</ColorPalette>

### Service Colors
Skate uses service colors to show users information about service, including
adherence and transit type. With exceptions for black, white, and grays,
these colors should not be used anywhere else in the UI.

<ColorPalette>
  <ColorItem
    title="Service Colors"
    colors={serviceColors}
  />
  <ColorItem
    title="MBTA Colors"
    colors={mbtaColors}
  />
</ColorPalette>

### All Colors
To avoid increased file sizes, we don’t create
text or background color classes for each of these variables.
Instead, we choose a subset of these colors for a theme palette.

All of the values in the theme palette are at the 500 level.

<ColorPalette>
  <ColorItem
    title='Eggplant'
    colors={allColors.eggplant}
  />
  <ColorItem
    title='Kiwi'
    colors={allColors.kiwi}
  />
  <ColorItem
    title='Strawberry'
    colors={allColors.strawberry}
  />
  <ColorItem
    title='Lemon'
    colors={allColors.lemon}
  />
  <ColorItem
    title='Gray'
    colors={allColors.gray}
  />

  #### Misc Colors
  <ColorItem
    title='System'
    colors={allColors.systemState}
  />
</ColorPalette>
