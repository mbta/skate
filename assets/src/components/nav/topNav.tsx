import React from "react"
import { logoIcon } from "../../helpers/icon"

const TopNav = (): JSX.Element => {
  return <div className="m-top-nav">{logoIcon("m-top-nav__logo")}</div>
}

export default TopNav
