import { createContext } from "react"

/**
 * Provides the enabled or disabled state of the map street view mode.
 */
const StreetViewModeEnabledContext = createContext(false)

export default StreetViewModeEnabledContext
